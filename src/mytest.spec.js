
// This function you have to include what you expect and the function that will run the test

it("result of the sum 2 + 2 must be 4", () => {
    const a = 2
    const b = 2
    const result = a + b

    expect(result).toEqual(4)
});